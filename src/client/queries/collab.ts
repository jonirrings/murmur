// Collab query hooks
// WebSocket connection and participant tracking are handled by useCollabEditor.
// This file is reserved for future TanStack Query hooks (e.g., room info, session management).

export {};
